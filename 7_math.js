//Math - built in object that provides collection of properties and methods

console.log(Math.PI)
console.log(Math.E)

let x = 3.21;
let y = 2;
let z;

z = Math.round(x); //rounds down the number - 3
z = Math.floor(x); //rounds down the number - 3
z = Math.ceil(x); //rounds up the number - 4
z = Math.trunc(x); //eliminates decimal portion - 3
z = Math.pow(x,y); //x to the power of y
z = Math.sqrt(x); //results in square root of the num
z = Math.log(x); //results in logrithmic of the num
z = Math.abs(x); //eliminating the negative sign

let max = Math.max(x, y)
let min = Math.min(x,y)
console.log(z)